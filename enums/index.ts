export enum StorageKey {
    Token = 'user_token',
    Userinfo = 'user_info'
}

export enum HttpStatusCode {
    OK = 200,
    BadRequest = 400,
    Unauthorized = 401,
    Unknown = 110,
    Timeout = 504
}